package main

func main() {
	// sg := make(chan os.Signal, 1)
	// signal.Notify(sg, syscall.SIGINT, syscall.SIGQUIT, syscall.SIGTERM)
	// n := cluster.Node{Options: cluster.Options{
	// 	IsMaster: true,
	// }}
	// log.Println(n.Startup())
	// s := <-sg
	// log.Println("Nano server got signal", s)
	// n.Shutdown()
}
